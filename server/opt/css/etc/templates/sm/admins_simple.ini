// Chogan admins (MISSION §4.3: non-Steam SteamIDs are forgeable → admins authenticate by NAME + PASSWORD or IP;
// never by SteamID). Rendered per instance by css-genconf; the live copy is /opt/css/etc/admins_simple.ini on the
// box (not in the repo). Format: "auth:identity" "flags" "password"  — with PassInfoVar "_password" the client sets
// setinfo _password <pw> before connecting.
//
// Examples (disabled):
// "name:chogan_op"     "99:z"   "CHANGE-ME-long-random-password"
// "ip:5.160.4.2"       "99:z"
